- name: Deploy with Docker Compose
  run: |
    echo "DOCKERHUB_USER=${{ vars.DOCKERHUB_USERNAME }}" >> .env
    echo "DOCKER_TAG=${{ github.sha }}" >> .env
    docker compose down || true
    docker compose up -d --force-recreate --pull always